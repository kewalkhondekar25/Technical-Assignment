// AppTheme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FEFEFC', // Adjust primary color as needed
    },
  },
  typography: {
    fontFamily: 'Arial, sans-serif', // Adjust font family as needed
  },
});

export default theme;
